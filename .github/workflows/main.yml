name: Edge AI — Build, Test e Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    name: "Lint & Tests"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar Dependências
        run: |
          pip install -r app/requirements.txt pytest ruff

      - name: Baixar Modelo e Criar Link para Testes
        run: |
          mkdir -p models
          wget -O models/yolov8n.pt https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt
          sudo mkdir -p /app
          sudo ln -sfn "$GITHUB_WORKSPACE/models" /app/models

      - name: Lint (Ruff)
        run: ruff check app/ --exit-zero

      - name: Executar Pytest
        run: pytest tests/ -v

  build:
    name: "Build & Push (ARM64)"
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar QEMU
        uses: docker/setup-qemu-action@v3

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build e Push — API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.api
          platforms: linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository }}/yolo-api:latest

      - name: Build e Push — Client (se houver Dockerfile.client)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.client
          platforms: linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository }}/yolo-client:latest

  model-gate:
    name: "Model Quality Gate"
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar Dependências
        run: pip install ultralytics

      - name: Baixar Modelo para Validação
        run: |
          mkdir -p models
          wget -O models/yolov8n.pt https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt

      - name: Executar Validação (mAP@0.5 >= 0.50)
        run: python scripts/validate_model.py

  deploy:
    name: "Deploy → Raspberry Pi 5"
    needs: model-gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Conectar à rede Tailscale
        uses: tailscale/github-action@v4
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}

      - name: Executar Deploy via SSH Nativo
        env:
          SSH_KEY: ${{ secrets.RPI_SSH_KEY }}
          HOST: ${{ secrets.RPI_HOST }}
          USER: ${{ secrets.RPI_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts

          ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no "$USER@$HOST" << 'EOF'
            set -e
            cd ~/yolo-edge-api
            git fetch origin main
            git reset --hard origin/main
            docker compose pull
            docker compose down --remove-orphans
            docker compose up -d --no-build
            docker image prune -f
          EOF
